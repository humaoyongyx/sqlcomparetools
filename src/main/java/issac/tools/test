(id,
 parent_id,
name,
hint,
icon,
command_id,
params,
menu_layout,
sort,
product_id,
command_type,
create_time,
hidden,
tag,
tool_key,
phase,
"&"tool_belong,
default_visible,
closely_visible,
reject_visible,
select_closely_visible,
select_reject_visible,
default_disabled,
closely_disabled,
reject_disabled,
select_reject_disabled,
select_closely_activated)
